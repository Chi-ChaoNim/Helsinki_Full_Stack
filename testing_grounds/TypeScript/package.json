{
  "name": "typescript",
  "version": "1.0.0",
  "description": "Place for learning TS",
  "license": "ISC",
  "author": "Chi",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "tsc": "tsc",
    "multiply": "tsc && node ./src/multiplier.ts",
    "calculate": "tsc && node ./src/calculator.ts",
    "start": "node index.ts",
    "dev": "concurrently \"tsc --watch\" \"node --watch index.ts\""
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "@types/node": "^26.1.0",
    "concurrently": "^10.0.3",
    "typescript": "^6.0.3"
  },
  "dependencies": {
    "express": "^5.2.1"
  }
}
